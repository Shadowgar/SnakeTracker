# Strict CSP

Result: **Pass**.

All scripts are local external assets. The policy contains no `unsafe-inline` or `unsafe-eval`.
Pinned Chart.js 4.5.1 and its license are vendored. The PWA registration and M6 screens ran in real
Chromium with zero console errors or warnings.
