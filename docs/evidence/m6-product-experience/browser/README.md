# Real-browser Product Experience

Result: **Pass** in Chromium on desktop and 390 by 844 mobile viewports.

An isolated Compose journey created a household and owner, opened Today, registered a Snake,
visited reports and analytics, searched by name, and verified a root-scoped service worker. Search
returned the keeper name/species/status/notes without raw event IDs or schema versions. Chromium
reported zero console errors and zero warnings on the final `e9b29fe` application image. The
production-mode journey also confirmed that the worker, rather than an HTTP request, advances M6
analytical projection checkpoints.

Retained screenshots: [Today](desktop-today.png), [desktop analytics](desktop-analytics.png),
[mobile analytics](mobile-analytics.png), [mobile reports](mobile-reports.png), and
[authorized search](desktop-search.png).
