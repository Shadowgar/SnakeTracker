# Measurement Analytics

Weight is shared where the capability profile permits it; length is Snake-only. Corrected,
voided, and reinstated measurements are derived from effective history. The versioned JSON endpoint
uses ETags and the visible table remains the chart's accessible equivalent.
